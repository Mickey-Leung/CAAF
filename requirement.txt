# Install from the repository root with Python 3.10 or newer.
.

# Numerical processing, sensor clustering, model training, and attribution.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
torch>=2.1
captum>=0.7

# Demo data loading, plots, and styled results tables.
pandas>=2
matplotlib>=3.7
jinja2>=3.1
h5py>=3.8

# Notebook interface, Python kernel, and notebook progress widgets.
jupyterlab>=4
ipykernel>=6
ipywidgets>=8
